# AI Editor — Roadmap

> Last updated: 2026-05-15 (2.56.0 shipped gitea#440 — hand-curated admit lists for chat.v1 / coder.v1 / kb.v1; closes the original github#40 paper-cut. Current released: **v2.56.0**; 2.50.0.1 / .2 / .3 / 2.51.0.1 sub-patches do not fire the release-readiness gate per the `X.Y.Z.N` convention. Recent shipped work: see [CHANGELOG.md](../CHANGELOG.md). The 2.33.0 → 2.44.0 audit-sweep arc closed at 2.44.0 with 22 inventory entries resolved across 11 system buckets; the inventory at [`audit-2026-Q2/inventory.md`](audit-2026-Q2/inventory.md) remains live as historical record + queue for the next sweep wave. Next forward step: ICD #4 finding #3 (`resolveReviewThread` dead-code decision — promote vs demote) still requires an architecture-decision session; ICD #5 (retrieval manager) shipped at `RE-EVAL following 2.52.0` left two queued findings (typedef widening on `ChunkStore`; `retrieval:turn-stats` shape-pinning test) — both still queued post-2.56.0; gitea#442 (`plugin.enabled` overlay flag) is the next code minor candidate, with the 2.55.0 `findAdmittingProfiles` `overlayNames` seam already in place.

## How to read this doc

Roadmap = **where we're going**. Forward cadence; what's queued; what's parked; what's open.

- Shipped work and per-PR rationale → [CHANGELOG.md](../CHANGELOG.md). Don't duplicate it here.
- System shape, layer boundaries, data flows → [ARCHITECTURE.md](ARCHITECTURE.md). (Stale; refresh scheduled — see §"Re-evaluation cadence.")
- Multi-version design arcs (preview, profiles, retrieval, etc.) → `docs/DESIGN-*.md`.
- X.Y.Z.N versioning convention for in-flight multi-PR work → [VERSIONING.md](VERSIONING.md).
- Pre-architecture thinking (questions parked behind triggers) → [`docs/discussion/`](discussion/).
- Audit + sweep queue → [`docs/audit-2026-Q2/inventory.md`](audit-2026-Q2/inventory.md).
- Dogfood + measurement battery → [`docs/dogfood-battery/`](dogfood-battery/) (per-session traces + README).

## How to read the bands

Every pending milestone in this doc carries a commitment-band label per the methodology adopted 2026-05-12 (see [`VERSIONING.md`](VERSIONING.md) for related X.Y.Z.N convention; the Plinth methodology at `/config/Projects/plinth/docs/METHODOLOGY-llm-assisted-development.md` is the source). For ai-editor:

- **`[strong]`** — load-bearing for the next 3 milestones. Code-ready: ICD or DESIGN-doc contract exists; a code session can pick this up and implement. Deviation requires an architecture session, not a code-session decision.
- **`[medium]`** — directional for the 4–7 milestone window. Shape is committed; precise wording tightens as the milestone approaches. A medium-band milestone needs a tightening session ahead of its code session.
- **`[fuzzy]`** — sketch for work beyond 7 milestones or with unknown preconditions. Narrative coherence only; not a constraint. A fuzzy-band milestone needs an architecture session first, then tightening, then coding.

**Re-evaluation cadence: every 3 code minors** (see §"Re-evaluation cadence" below). Re-eval slots are doc-only and accumulate in `[Unreleased]` — they do **not** consume a version slot. The cadence counts code minors only; the slot in between does not bump. The cadence keeps the sliding window aligned: content promotes (fuzzy→medium→strong) as milestones approach; content demotes or relocates as milestones slip.

**Unlabeled = strong.** New roadmap rows added without an explicit band are implicit `[strong]` — the methodology says that commitment should feel heavy. If in doubt, label `[medium]` and tighten at the next re-eval.

---

## Now / Next / Later

| Phase | Track | Band |
|---|---|---|
| **Now (next up)** | **One open architecture-decision item + two ready code rows:** (a) **ICD #4 finding #3 — `resolveReviewThread` dead-code decision** (promote vs demote). Declared at [`base.js:594`](js/git-providers/base.js); no provider overrides; `threadResolve` capability is `false` everywhere; no UI read site. Decision-required. (b) **gitea#442 — `plugin.enabled` overlay flag** — capability overlay mirroring `scriptAutomation.enabled` / `preview.enabled`; admits `PLUGIN_TOOL_NAMES` onto any active profile when flipped on. Decided at `discussion/plugin-dev-mode-vs-profile.md`. The 2.55.0 `findAdmittingProfiles` already accepts the `overlayNames` seam — this row is the data + Settings UI wire-up. (c) **Two ICD #5 queued findings** — `ChunkStore` typedef widening to absorb the 12 `@ts-ignore` annotations in `manager.js` + `retrieval:turn-stats` shape-pinning test (browser-DOM-coupled). Mechanical; bundle when one rises. | `[medium]` (decision-required) + `[strong]` + `[strong]` × 2 |
| ~~**Just shipped (2.56.0)**~~ | ~~**Hand-curate admit lists for chat.v1 / coder.v1 / kb.v1 (gitea#440); closes github#40 paper-cut.**~~ ✅ shipped 2026-05-15. The 2.54.0 byte-equivalent migration carried the github#40 paper-cut: `create_issue` reachable from picker chat via a `'pm'` tag the user never saw. This pass relocates the issue-write cohort (`create_issue`, `update_issue`, `add_issue_comment`) from `chat.v1` to `coder.v1` (paper's "where it actually belongs" framing); drops six additional out-of-purpose entries from `chat.v1` (`create_pull_request`, `merge_pull_request`, `add_pr_review`, `delegate_task`, `set_active_project`, `submit_script_for_approval`, `sync_releases`); and aggressively trims `kb.v1` to read-only-by-construction — admit list now agrees with `KB_SYSTEM_PROMPT`'s read-only constraint, drops the `'mcp__*'` glob (MCP servers may be mutating; trust boundary requires explicit per-tool admission per `subagent.v1`'s precedent). Net counts: chat.v1 = 51 + `mcp__*`; coder.v1 = 73 + `mcp__*`; kb.v1 = 33 (no glob). Bundled regression-prevention test source-scans every `(?:ToolRegistry\|registry).register('name', ...)` call in `js/tools/*.js` and asserts `Profiles.findAdmittingProfiles(name).length > 0` — the CI-gate mirror of the 2.55.0 boot-warn. Closes gitea#440. Admin: gitea#438 and gitea#439 closed (their PRs shipped using `(gitea#N)` reference syntax rather than `closes gitea#N`). | `[strong] [S]` (shipped) |
| ~~**Just shipped (2.55.0)**~~ | ~~**Default-OFF dev warning when newly-registered tools aren't admitted by any profile (gitea#439).**~~ ✅ shipped 2026-05-15. The 2.54.0 inversion accepted *default-OFF* as the cost of fixing silent over-admission. This row surfaces the failure mode loudly at registration time: `ToolRegistry.register` now calls a new `Profiles.findAdmittingProfiles(name)` after the store updates, on the fresh-register branch only (HMR / MCP-reconnect antibody), and emits one `console.warn` if zero profiles admit the new tool. The `'*'` sentinel in `full.v1` deliberately does NOT count — a tool reachable only via the bypass is invisible to picker profiles, which is precisely the silent-vanish case this guards against. Generic warning text suggests "add to profile X.tools.admit (e.g. chat.v1, coder.v1, kb.v1)" — no heuristic profile guess (prefix heuristics mis-classify constantly; `create_issue` chat-side vs `create_file` coder-side). The helper accepts an `overlayNames?: string[]` forward-compat seam so gitea#442's `PLUGIN_TOOL_NAMES` overlay is a one-line wire-up. 17 new subtests across two new test files; full suite 3467 pass / 1 skipped / 0 fail. Unblocks regression-safe execution of gitea#440 (hand-curate admit lists) — over-trimming surfaces at boot. | `[strong] [S]` (shipped) |
| ~~**Just shipped (2.54.0)**~~ | ~~**Invert tool admission (gitea#438) — tool-side `roles:` retired; profiles enumerate explicit names in `tools.admit`.**~~ ✅ shipped 2026-05-15. The github#40 paper-session (closed 2026-05-15) decided to invert admission. Pre-2.54.0 tools self-tagged with `roles:` and profiles intersected via `allowed_groups`; the runtime gate (`Profiles.filterTools`) admitted on tag overlap. Three failure modes drove the inversion: silent over-admission via invisible tags (`create_issue` reachable from picker chat via `'pm'`); silent dead tools (`'plugin-dev'` admitted by zero picker profiles); diverging-not-narrowing inheritance. New contract: `profile.tools.admit: string[]` enumerates names; `'*'` is the bypass sentinel (full.v1); `'<prefix>__*'` glob entries admit MCP tools by prefix; `admit_add` / `admit_remove` operators let children narrow/widen without restating. All 10 profile files migrated; ~30 tool files swept of `roles:`; `getKnownGroupTags` + `LEGAL_GROUP_TAGS` validation + `_registeredRoles` enrichment retired; composer's now-no-op `user_groups` filter passes-through. **Profile-side `Profiles.filterTools` is the sole admission gate.** New tests pin per-profile admit baselines + `admit_add`/`admit_remove` narrowing-not-diverging invariant. Unblocks gitea#439 (default-OFF dev warn), gitea#440 (hand-curate admit), gitea#442 (`plugin.enabled` overlay). gitea#441 (Roles→Profiles UI rename) and gitea#443 (Phase 4 user-authoring API placeholder) remain independent. | `[strong]` (shipped) |
| ~~**Just shipped (RE-EVAL following 2.52.0)**~~ | ~~**Fifth re-eval slot — doc-only, no version bump.**~~ ✅ ran 2026-05-15. ICD #5 authored at [`docs/ICD-retrieval-manager.md`](ICD-retrieval-manager.md) — retrieval manager + ingest pipeline + lifecycle + persistence + diagnostics contract; 5 classification axes (Ingest / Lifecycle / Persistence / Query / Diagnostics); covers the upstream pipeline that feeds the Composer (ICD #2 stays in scope for the `compose()` algorithm itself). ARCHITECTURE.md catch-up sync 2.49.0 → 2.52.0 (3-minor delta: 2.50.0 ICD #4 findings + 2.50.0.1 Venice prompt-cache + 2.50.0.2 tool-surface UX + 2.50.0.3 cross-request cache + 2.51.0 compact TOC + 2.51.0.1 cut-off reason + 2.52.0 `read_approved_plan`). Administrative sweep: closed gitea#418/#424/#425/#426 (the four shipping-PR-didn't-close-the-issue stragglers). Three `[strong]`-band code-aware findings surfaced — `resolveTaskLedgerConfig` (suggested for next code minor; mirrors 1.20.0 retrieval-config pattern), `ChunkStore` typedef widening to absorb the 12 `@ts-ignore` annotations in manager.js (queued), and a `retrieval:turn-stats` shape-pinning test (queued; browser-DOM-coupled). Next re-eval slot is `RE-EVAL following 2.55.0`. | `[rewrite session]` (re-eval) |
| ~~**Just shipped (2.52.0)**~~ | ~~**`read_approved_plan` tool + persisted plan body (gitea#424).**~~ ✅ shipped 2026-05-15. Plan-Mode sessions now persist the approved plan body in a new `State.approvedPlan` slot, surfaced back to the model via a read-only `read_approved_plan` tool. Closes the last open AAR-cohort issue from the 2026-05-14 `qwen-3-6-plus` session against [xcaliber/HTML-Games#215](https://git.gobha.me/xcaliber/HTML-Games/issues/215). Net effect: the executor stops regenerating identical DESIGN.md content from the inlined plan body — the plan now lives in one read-only place. | `[strong]` (shipped) |
| ~~**Just shipped (2.51.0.1)**~~ | ~~**In-track patch — session cut-off reason surfaced (gitea#425).**~~ ✅ shipped 2026-05-15. Session terminations now carry a structured `reason` field on the trailing assistant message + status pill (`budget-exceeded` / `model-finished` / `user-cancelled` / `tool-cancelled` / etc.). Users (and the model on a Plan-Mode follow-up) can distinguish "we ran out of budget" from "the model stopped cleanly." | `[strong] [S]` (shipped) |
| ~~**Just shipped (2.51.0)**~~ | ~~**Compact tools TOC in system prompt (gitea#426).**~~ ✅ shipped 2026-05-15. Replaced verbose per-tool enumeration with a compact category index in the system prompt's stable head; the model reads names + groupings from the TOC without consuming the full tool-by-tool prose every turn. Cuts repeat `list_tool_categories` / `list_tools_by_category` calls. Full per-tool detail still available via discovery tools when the model needs it. | `[strong]` (shipped) |
| ~~**Just shipped (2.50.0.3)**~~ | ~~**In-track patch — cross-request read-cache returns full payload (gitea#421).**~~ ✅ shipped 2026-05-15. The cross-request `_cache_note` path on stateful reads (file content, search results, etc.) now returns the full cached payload rather than a "(cached)" marker when the args match. Same eviction-vs-read tension that produced the 3rd instance of the cache-invalidation-on-mutation antibody (`project_edit_file_stale_cache_deadlock` memory). | `[strong] [S]` (shipped) |
| ~~**Just shipped (2.50.0.2)**~~ | ~~**In-track patch — tool-surface UX (gitea#422).**~~ ✅ shipped 2026-05-15. Origin: same 2026-05-14 `qwen-3-6-plus` AAR against [HTML-Games#215](https://git.gobha.me/xcaliber/HTML-Games/issues/215) as 2.50.0.1 (#423). Three tools (`create_file`, `read_lines`, `search_in_files`) each cost a round-trip when the model called them with cross-SDK-prior arg names (`start`/`end`, `pattern`) or omitted a param the description didn't advertise as required. Three composable fixes mirroring 2.48.0.1 / gitea#415: (a) [`js/chat/tools.js`](../js/chat/tools.js) — new `TOOL_PARAM_ALIASES` frozen export (`start`→`start_line`, `pattern`→`query`, etc.) + `applyAliasesAndDefaults` pure rewrite + wire-in at `executeToolCall` before `validateToolParameters` fires; (b) `**Required:** <comma-list>.` prefix on the `description` of every tool in `REQUIRED_TOOL_PARAMS` (18 tools across `project-tools.js` / `scan-tools.js` / `file-tools.js` / `search-tools.js` / `edit-tools.js` / `issue-tools.js` / `pr-tools.js`); (c) `REQUIRED_TOOL_PARAMS['create_file']` collapsed `['path', 'content', 'message']` → `['path', 'content']` to align with the JSON schema + description + handler default that already advertised `message` as optional. Two new test files: [`tests/test-chat-tools-validation-aliases.mjs`](../tests/test-chat-tools-validation-aliases.mjs) (17 subtests covering rewriting, canonical-wins-over-alias, invariants, `_detectWrongShape` composition) + [`tests/test-tool-description-required-prefix.mjs`](../tests/test-tool-description-required-prefix.mjs) (pinning test, same source-scan idiom as `test-chat-tool-name-literals.mjs`); one new subtest in `tests/test-chat-tools-validation-message.mjs`. Four AAR-cohort siblings (#421, #424–#426) remain candidates for future in-track patches. | `[strong] [S]` (shipped) |
| ~~**Just shipped (2.50.0.1)**~~ | ~~**In-track patch — Anthropic-style prompt-cache breakpoint on Venice requests (gitea#423).**~~ ✅ shipped 2026-05-15. Origin: AAR of a `qwen-3-6-plus` session against [HTML-Games#215](https://git.gobha.me/xcaliber/HTML-Games/issues/215) — 918k tokens / $0.62 / 42 requests; ~209k tokens (~23% of session) were the Composer-trimmed tool-defs prefix being re-billed every request. [`js/providers/venice.js`](../js/providers/venice.js) lines 12-26 + 112-118 — `transformRequest` now wraps the last system message's content as `[{type:'text', text, cache_control:{type:'ephemeral'}}]` so Venice's Anthropic-style prefix cache retains `(tools + system)` across the session; module-local `_attachSystemCacheBreakpoint` handles find-last-system + idempotency + empty-string guards. New `enablePromptCache` setting (default ON, surfaced in Settings → Provider → Venice). Scoped to Venice — other providers extend via the same seam later. Cache-hit tokens already surface through `extractUsage()` → `State.sessionCost`, so dashboard savings are automatic with no new UI. 7 new subtests at [`tests/test-venice-prompt-cache.mjs`](../tests/test-venice-prompt-cache.mjs). | `[strong] [S]` (shipped) |
| ~~**Just shipped (2.50.0)**~~ | ~~**ICD #4 code-aware findings #1 + #2.**~~ ✅ shipped 2026-05-14. (a) [`js/git-providers/base.js`](../js/git-providers/base.js) lines 637–650 — `async getCommitDiff(connection, owner, repo, sha)` promoted to `BASE_GIT_PROVIDER` with the `notSupported` default; no change to the three concrete providers, their existing overrides satisfy the contract. (b) [`tests/test-provider-capabilities-shape.mjs`](../tests/test-provider-capabilities-shape.mjs) NEW — pins the six-flag capability contract (`reviewSubmission`, `threadResolve`, `viewedFiles`, `merge`, `rerunCi`, `mergeConflictResolution`) across base + github + gitea + gitlab + local via the registry's shallow-spread shape; second test pins no-extras. (c) [`js/git-providers/gitlab.js`](../js/git-providers/gitlab.js) lines 1161–1172 — capabilities getter declares all six flags explicitly (was just `mergeConflictResolution: true`); the other five flip from default-undefined to explicit `false`. (d) [`tests/test-pr-review-provider-shape.mjs:57-71`](../tests/test-pr-review-provider-shape.mjs) updated to pin the new GitLab explicit-flag shape. Absorbs the `RE-EVAL following 2.49.0` doc-only deliverables. ICD #4 finding #3 (`resolveReviewThread` dead-code decision) deferred — decision-required. | `[strong] [S]` (shipped) |
| ~~**Previous re-eval slot (RE-EVAL following 2.49.0)**~~ | ~~**Fourth re-eval slot — doc-only, no version bump.**~~ ✅ ran 2026-05-14. ICD #4 authored at [`docs/ICD-git-providers.md`](ICD-git-providers.md) (`BASE_GIT_PROVIDER` 55-method surface + 4 concrete providers + circuit breaker). Paper-half: 43-method → 55-method drift fix across ROADMAP + ARCHITECTURE + DESIGN-git-providers. Three code-aware findings queued; findings #1 + #2 ✅ resolved at 2.50.0 (this row above); finding #3 (`resolveReviewThread` dead-code decision) still open. After this slot: Touch 3 Window v2 / Sessions and github#27 OAuth flows remain `[medium]` until their DESIGN docs land. Next re-eval slot is `RE-EVAL following 2.52.0` (3 code minors past 2.49.0). | `[rewrite session]` (re-eval) |
| ~~**Just shipped (2.49.0)**~~ | ~~**Sub-agents Phase 1 end-to-end (github#24 Phase 1 — slice 2 of two; tag `v2.49.0` fired here).**~~ ✅ shipped 2026-05-14. Slice 2 of the 2.49.0 minor — Phase 1 sub-agents per [`docs/DESIGN-sub-agents.md`](DESIGN-sub-agents.md). The `delegate_task` tool + `SubAgentApprovalCard` pair + `SubAgentTranscriptPanel` + `coder.v1.tools.static` admission + `SUBAGENT_INSTRUCTIONS` prompt block + cost-attribution threading (`LLM.chat` `costAttribution` arg) + sub-agent runner glue (uses `tool-loop-core.runToolLoop`) + persistence (`conv-{id}.subagentTranscripts`) + Settings UI row + browser test. End-to-end; the model can call `delegate_task`, the approval card mounts with a security-load-bearing capability summary, the user approves, a sub-agent loop runs against `subagent.v1` (restrictive read-only default), and a structured result envelope returns to the parent. Slice 1 (`v2.49.0.0`, pre-tag plumbing) landed earlier the same day — see CHANGELOG [2.49.0]. | `[strong]` (shipped) |
| ~~**Previous in-track patch (2.49.0.0)**~~ | ~~**Slice 1 of github#24 Phase 1 sub-agents.**~~ ✅ shipped 2026-05-14. Pure plumbing; zero user-observable surface. `subagent.v1` profile in `SYNTHETIC_ENTRIES`; `resolveSubAgentConfig`; `ToolRegistry.executeWithProfile` + `checkRoleAccessForProfile` (additive entry-points; existing `execute` / `checkRoleAccess` delegate); `applySubAgentToolFilter` in the `getToolsForRole` composition chain (no-op against unregistered `delegate_task`); `State.subagents = {tree, transcripts, session_cost}` slot; `pendingSubAgentApproval` + helpers in `js/chat/state.js` (mirror `pendingScriptApproval`); `cancelToolLoop` releases an in-flight sub-agent approval. `'delegate_task'` joins `USER_PAUSE_TOOLS`. 23 new subtests across 3 test files + 2 pinning tests absorbed the new entries. Full Node suite 3326 pass / 1 skipped / 0 fail. | `[strong] [S]` (shipped) |
| ~~**Just shipped (2.48.0.1)**~~ | ~~**In-track patch — file-edit tool surface (gitea#415).**~~ ✅ shipped 2026-05-14. Origin: AAR of a `qwen-3-6-plus` session on [HTML-Games#212](https://git.gobha.me/xcaliber/HTML-Games/issues/212) — ~30% wasted budget recovering from opaque `edit_file` errors. Three fixes bundled: (a) [`js/tools/multifile-tools.js`](../js/tools/multifile-tools.js) lines 119–146 — `_detectWrongShape()` gains an `edits`-key branch + echoes the first parsed entry back as `parsed_edits` so the model sees its own intent surfaced; (b) [`js/tools/multifile-tools.js`](../js/tools/multifile-tools.js) line 308 — `edit_file` description drops the misleading "Preferred over open_file + replace_lines" framing, adds a concrete example payload, and flips polarity so `replace_lines`/`insert_lines`/`delete_lines` are advertised as the niche stateful path; (c) [`js/chat/tools.js`](../js/chat/tools.js) lines 67–79 — `validateToolParameters()` drops the misleading "This usually happens when the AI response was truncated" hypothesis; plain "missing required parameter(s) + required-for set" lets the caller act on `missingParams`/`providedArgs` directly. Two new tests + 3 added to the refusal-hint suite; 20/20 pass. `replace_lines.path` / `read_lines.path` optionality explicitly rejected as out-of-scope (would create a fourth state model — `project_constraints.md` single-global-state). | `[strong] [S]` (shipped) |
| ~~**Previous code minor (2.48.0)**~~ | ~~**Tool-loop core extraction — github#24 Phase 0.**~~ ✅ shipped 2026-05-14. `handleGeneralRequest`'s iterative tool loop refactored into a reusable `runToolLoop(context, hooks, transport)` in [`js/chat/tool-loop-core.js`](../js/chat/tool-loop-core.js) (NEW, ~720 LOC) so Phase 1 sub-agents (`delegate_task` tool, target 2.49.0) can drive the same loop with a different context + hooks bag. 13 DOM/state-coupling sites in [`js/chat/handlers.js`](../js/chat/handlers.js) collapsed onto hooks; `handleGeneralRequest` shrunk from ~820 to ~210 lines. 22 pinning subtests at [`tests/test-tool-loop-core.mjs`](../tests/test-tool-loop-core.mjs). No behavior change. | `[strong]` (shipped) |
| ~~**Previous code minor (2.47.0.2)**~~ | ~~**Second-pass retrieval `No runtime wire-up` docstring sweep.**~~ ✅ shipped 2026-05-14. Five files missed by 2.47.0.1's bundled sweep. | `[strong] [S]` (shipped) |
| ~~**Previous code minor (2.47.0.1)**~~ | ~~**Bundled docstring cleanup — sibling-file `js/context-manager.js` reference cleanup across `js/intelligence/retrieval/` + stale `getToolsForProfile` docstring fix.**~~ ✅ shipped 2026-05-14. 15 retrieval files / 27 stale references caught up to the 1.5.14 cutover (`index.js`, `pipeline.js`, `bm25-indexer.js`, `loader.js`, `ledger-consumer.js`, `measurement.js`, `strategies/semantic.js`, `strategies/thematic.js`, `test-corpus.js`, `manager.js`, `walker.js`, `comparison.js`, `wiring.js`, `ingest-controller.js`, `embedder.js`) + [`js/tools/registry.js`](../js/tools/registry.js) lines 253–271 docstring rewrite. Mirrors the 2.46.0 retrieval-Composer fix shape. Absorbed the third RE-EVAL slot's accumulated `[Unreleased]` content per Decision §14 sub-clause. | `[strong] [S]` (shipped) |
| ~~**Previous code minor (2.47.0)**~~ | ~~**Runtime regression fixes — `window.startWorkOnIssueFromList` (issue-list Start button) + `Providers.enrichModels` (core.js facade gap).**~~ ✅ shipped 2026-05-14. Two stale-reference regressions surfaced from a user diagnostic dump on a live 2.46.0 session. [`js/app.js`](../js/app.js) lines 836–843 + [`js/core.js`](../js/core.js) lines 1425–1438; two regression tests added at [`tests/test-app-issue-list-wiring.mjs`](../tests/test-app-issue-list-wiring.mjs) and [`tests/test-core-providers-facade.mjs`](../tests/test-core-providers-facade.mjs). Pre-approved 2.47.0 docstring-cleanup row re-scoped to 2.47.0.1. | `[strong] [S]` (shipped) |
| ~~**Just shipped (RE-EVAL following 2.46.0)**~~ | ~~**Third RE-EVAL slot — doc-only, no version bump.**~~ ✅ ran 2026-05-14. Deliverables in `[Unreleased]`: (a) third ICD authored at [`docs/ICD-tool-registry.md`](ICD-tool-registry.md) (Tool registry admission contract — 11 exports, 5 axes, three carve-outs); (b) ARCHITECTURE.md catch-up sync 2.44.0 → 2.46.0; (c) paper-half drift fixes (this row + cadence renames + Decision §14 sub-clause). Code-aware findings: one drift item — stale `ToolRegistry.getToolsForProfile` docstring claiming a retired deprecation shim is "preserved below" — queued for 2.47.0+. | `[rewrite session]` (re-eval) |
| ~~**Previous code minor (2.46.0)**~~ | ~~**2.46.0 — retrieval Composer stale docstring fix.**~~ ✅ shipped 2026-05-14. Single-file fix at [`js/intelligence/retrieval/composer.js`](../js/intelligence/retrieval/composer.js) lines 31–35 — replaced "No runtime wire-up" (stale since 1.5.14) with "Production wiring (since 1.5.14)" + forward-pointer to [`ICD-intelligence-composers.md`](ICD-intelligence-composers.md). Audit pass: lines 31–34 were the only drift in `composer.js`. ~12 sibling files queued for 2.47.0 (see Now above). | `[strong]` (shipped) |
| **Later** | **Tier 3b in-editor preview** — sidecar + build-step support (Playwright per workspace, container isolation); gating probe not operational. github#37 Phase 2 (re-scope from dogfood signal); github#27 Phase 3 self-hosted templates; github#24 sub-agents Phase 1 (DESIGN shipped 2.37.0); github#18 cross-device sync (post-2.0). | `[medium]` / `[fuzzy]` per row in §"Known open issues" |
| **Deferred** | Foundations (was 1.1.x), Compression Rules 3–5, various UI items — see §"Deferred / parked" below. | `[fuzzy]` per row |

---

## TL;DR — architectural commitments

The four DESIGN subsystems (`docs/DESIGN-*.md`) describe an intelligence-layer rebuild — retrieval, memory, compression, tools — coordinated by per-surface profiles. Three load-bearing commitments:

1. **Admissibility, not accumulation** — every byte the model sees has earned its place.
2. **Git-native memory** — memory and conversation state can opt into living in `.aieditor/*` files committed with the repo. No backend; notes follow the code across machines and forks.
3. **Measurement before scale** — the cost dashboard ships with the first eviction subsystem, not at the end of the arc. Each subsequent track lands against measured baselines.

A 2.0 ships when profiles become the load-bearing configuration surface. **The 2.0 cut shipped at 2.0.0 (PR #322 sequence); the role selector retired with it.** The "what's still open" view of the 2.X path is in §"2.X path — what's still open" below.

---

## Cadence and versioning

**SemVer with intent.**

| Bump | Triggered by |
|---|---|
| Patch (`X.Y.Z → X.Y.Z+1`) | Bug fixes, doc updates, security, small UI polish, single-rule additions inside an active track |
| Minor (`X.Y → X.(Y+1)`) | New subsystem track lands Phase 1, or a self-contained feature spans multiple files |
| Major (`X.Y → (X+1).0`) | Load-bearing surface change (the 2.0 cut was the profile contract becoming load-bearing) |

**The X.Y.Z.N sub-patch convention.** Multi-PR / multi-slice work develops in `X.Y.Z.N` space (sub-patch) until tag-ready; the single `vX.Y.Z` tag fires when the feature is testable end-to-end. Full convention + composition with the existing versioning feedback memories: see [`VERSIONING.md`](VERSIONING.md).

**Branching.** Main is protected. Every change goes through a PR from a topic branch. Gitea CI runs security lint + Docker build on every PR; tag push deploys to production. PR title convention: `feat(track):`, `fix(area):`, `chore(release):`, `docs(...)`. Squash + delete on merge. No "preview" or "beta" channels — `:dev` Docker tag for PRs and `:test` for `main` provide preview environments.

**Release-readiness gate (added 2026-05-04, retroactive from `v1.6.0`).** Gates **release tag pushes** (`X.Y.Z`-shaped only — `X.Y.Z.N` sub-patches don't fire the gate). Drive a real chat session of ~10 turns with tool calls, run `find_relevant_files` against a query, make a code edit + commit, all in this repo. No silent history truncation; no broken-history 400s; no stale-state regressions in surfaces touched since the previous tag. Honor-system today; the dogfood result is recorded on the release tag annotation alongside the bundled-PR list. **Why this exists:** between 1.5.5 and 1.5.13 the retrieval track shipped seven internally-correct minor versions while the chat surface had a known summarizer regression that made the editor unusable for long sessions. The X.Y.Z.N convention is the upstream slowdown; the gate is the downstream check.

---

## Re-evaluation cadence

Per methodology §Phase 3, every 3 code minors past the next-N window get a re-evaluation slot. Re-eval items appear inline in forward cadence here, distinguished from code milestones by the `[rewrite session]` tag (no band label — re-eval isn't code work). **Re-eval slots are doc-only and accumulate in `[Unreleased]`; they do not consume a version slot** (policy refinement adopted at `RE-EVAL following 2.46.0`; see also Decision §14). The first re-eval slot is anchored at the next milestone boundary post-2.38.0; subsequent slots fire every 3 code minors thereafter, sitting between code minors rather than replacing them.

**What each re-evaluation does** (both halves run unless explicitly noted; default is run both):

- **Paper half** — promote `[fuzzy]` content whose preconditions materialized; tighten `[medium]` content whose milestone window is approaching; demote or relocate content whose milestone slipped. Update milestone band labels to reflect dependency changes. Verify the Forward ICD presence check (§ below) for the next-3 `[strong]` window.
- **Code-aware half** — read `js/` against the ARCHITECTURE.md claims; find architectural decisions embedded in code comments that should be promoted; find new interfaces in code that aren't in any DESIGN doc; find tests missing for `[strong]`-band claims. Produce a gap analysis that feeds back here.

**~~First re-eval slot: `RE-EVAL following 2.41.0`~~** — ✅ ran 2026-05-12 as the 2.42.0 doc-only PR. *(Pre-policy-refinement: this slot was versioned. Going forward, re-eval slots accumulate in `[Unreleased]` without a version bump — see Decision §14 sub-clause.)* Deliverables shipped: (a) **ARCHITECTURE.md refresh** to 2.41.0 sync (was 1.9.1 sync — 32 minors behind) including new sections for Touch 3 surfaces, slot/event registries, audit-sweep wave outcomes, refreshed File Size Map, and a corrected Testing & CI section; (b) **first ICD** authored at [`ICD-chat-handlers.md`](ICD-chat-handlers.md) — chat tool-loop 5-axis classification contract per §"Per-subsystem ICD backfill program" target #1; (c) **paper-half roadmap drift fixes** below (header re-sync, gitea#392 / gitea#393 strikethrough, panels-manifest promotion to next-3 window).

**~~Second re-eval slot: `RE-EVAL following 2.44.0`~~** — ✅ ran 2026-05-14 as the 2.45.0 doc-only PR. *(Last of the pre-policy-refinement versioned re-evals. The 2.42.0 + 2.45.0 doc-only releases established the re-eval pattern; the no-version-bump rule adopted at the third slot corrects forward without rewriting their history.)* Deliverables shipped: (a) **ARCHITECTURE.md catch-up sync** from 2.41.0 → 2.44.0 (4-minor delta: 2.42 first re-eval + ICD, 2.43 rail-views migration, 2.44 audit-sweep wave close) — added DomBindings (2.44.0.1) + TabActivationRegistry (2.44.0.2) + Chat tool-name pin (2.44.0.0) + `managers/` retirement (2.44.0.3); ICD-backfill row updated. (b) **Second ICD** authored at [`ICD-intelligence-composers.md`](ICD-intelligence-composers.md) — Tools + Retrieval Composer seam contract per target #2; 5 classification axes (Source/Budget/Authorization/Overflow/Diagnostics) over the two pure-function Composers; cross-references ICD-chat-handlers §"Composer-vs-non-Composer path drift" for the system-prompt parallel. (c) **Paper-half roadmap output:** Touch 3 Window v2 / Sessions and github#27 OAuth flows decisions; one `[strong] [S]` row for 2.46.0 (retrieval Composer stale docstring — the only drift surfaced).

**~~Third re-eval slot: `RE-EVAL following 2.46.0`~~** — ✅ ran 2026-05-14. Doc-only — **no version bump** (policy refinement adopted this slot; deliverables accumulated in `[Unreleased]` until 2.47.0.1 absorbed them). Deliverables: (a) **third ICD** authored at [`docs/ICD-tool-registry.md`](ICD-tool-registry.md) — Tool registry admission contract per target #3 (`LEGAL_GROUP_TAGS` derivation at 2.34.0 + `roles: 'all'` / `allowed_groups: '*'` carve-outs + `'full'` legacy-bypass tag); 11 exports, 5 classification axes, three carve-outs documented. (b) **ARCHITECTURE.md catch-up sync** from 2.44.0 → 2.46.0 (header bump + new tools-registry ICD pointer + ICD-hierarchy row + Slot & Event Registries section-header range fix). (c) **Paper-half roadmap output:** policy refinement (re-eval slots don't consume a version), cadence-language tightening, Now/Next/Later restructure, Decision §14 sub-clause. **Code-aware finding ✅ resolved at 2.47.0.1:** the one drift item — `ToolRegistry.getToolsForProfile` docstring claiming a retired deprecation shim "is preserved below" (registry.js:265–267) — was bundled with the sibling-file `context-manager.js` cleanup in 2.47.0.1.

~~**Next re-eval slot: `RE-EVAL following 2.49.0`**~~ — ✅ ran 2026-05-14. ICD-backfill target #4 (`git-providers/base.js` 55-method base interface) authored at [`docs/ICD-git-providers.md`](ICD-git-providers.md); paper-half drift + admin-cleanup folded in. ~~**`RE-EVAL following 2.52.0`**~~ — ✅ ran 2026-05-15. ICD-backfill target #5 (retrieval manager + ingest pipeline + lifecycle + persistence + diagnostics) authored at [`docs/ICD-retrieval-manager.md`](ICD-retrieval-manager.md); ARCHITECTURE.md catch-up sync 2.49.0 → 2.52.0; administrative sweep (closed gitea#418/#424/#425/#426 — shipping-PR-didn't-close stragglers); three `[strong]`-band code-aware findings surfaced (one suggested for the next code minor's `[strong]` row: `resolveTaskLedgerConfig` resolver mirroring 1.20.0; two queued). **Next re-eval slot: `RE-EVAL following 2.55.0`** — anchored per the 3-code-minor cadence (3 code minors past 2.52.0 = 2.55.0). Target #6 candidates: editor instance, MCP bridge, plugin lifecycle (profiles registry deferred per `project_profile_admission_paper` memory — paper in flight at [`discussion/`](discussion/)).

**Subsequent re-evals.** Inline as `RE-EVAL following 2.52.0`, `RE-EVAL following 2.55.0`, etc. — every 3 code minors. Skip the code-aware half only when nothing meaningful has merged since the last pass (the methodology's "structural-only" exception). Default is run both. **All re-evals from this slot forward accumulate in `[Unreleased]`** — they do not consume a version slot.

### Per-subsystem ICD backfill program

Existing code has accreted contracts that live as inline classification sets, registry filters, and base interfaces rather than as written ICDs. The 2026-Q2 audit sweep captured *symptoms* — hardcode walls, orphan events, duplicate implementations — but not *contracts*. The ICD backfill program covers the highest-drift subsystems, **one per re-eval slot.**

**Cadence.** One subsystem ICD per re-eval slot (every 3 minors), interleaved with the paper + code-aware halves above. Each session = one architecture conversation; output is `docs/ICD-<subsystem>.md` (or a contract section appended to the relevant `DESIGN-*.md` if one already exists), plus any code-aware findings that the ICD-writing process surfaces. **No code changes in the ICD session itself** — drift fixes spawn as `[strong]`-band roadmap items in subsequent code minors.

**Ordered backlog, highest-drift first** (each is `[medium]` until its re-eval slot opens it, then promotes to `[strong]`):

1. ~~**`chat/handlers.js` + classification axes**~~ — ✅ shipped 2.42.0 at [`ICD-chat-handlers.md`](ICD-chat-handlers.md). Covers 5 axes (not 4 as originally scoped — the actual classification matrix is `WRITE` / `STATEFUL_READ` (cache-skip), `FILE_MUTATING` / `PREVIEW_MUTATING` (eviction), `MUTATING` (envelope messaging), `WHOLE_FILE_WRITE` (FileOp metadata), `LONG_RUNNING` / `USER_PAUSE` (timeout)) across 8 frozen exports; the +1 axis-count emerged from reading the code against the original scoping. Code-aware findings produced **zero subsequent `[strong]` rows** — the contract was already coherent. Updated 2026-05-12.
2. ~~**Intelligence-layer Composer seam**~~ — ✅ shipped 2.45.0 at [`ICD-intelligence-composers.md`](ICD-intelligence-composers.md). Covers both `js/intelligence/tools/composer.js` (`composeAdmission` + `renderForLLM`) and `js/intelligence/retrieval/composer.js` (`compose`) under one seam contract; 5 classification axes (Source / Budget / Authorization / Overflow / Diagnostics) plus the "Composer-vs-non-Composer path drift" cross-reference to ICD-chat-handlers. Code-aware findings produced **one** `[strong] [S]` row for 2.46.0 (stale "not yet wired" docstring in retrieval Composer — production wiring landed at 1.5.14) — ✅ shipped 2.46.0 (docstring fix + audit pass confirming `composer.js` had no other drift). The 2.46.0 audit pass surfaced one **out-of-scope** sibling-file finding (15 retrieval-module files still referencing legacy `js/context-manager.js`) — ✅ resolved at 2.47.0.1 (bundled with the registry-docstring fix). Updated 2026-05-14.
3. ~~**Tool registry admission contract**~~ — ✅ shipped at `RE-EVAL following 2.46.0` at [`docs/ICD-tool-registry.md`](ICD-tool-registry.md). Covers `js/tools/registry.js` + `js/profiles/registry.js` under one admission contract; 11 exports, 5 classification axes (Declaration / Admission / Bypass / Failure / Diagnostics), three carve-outs (`'all'` tool side, `'*'` profile side, `'full'` legal-but-never-declared admin-tier tag). Code-aware findings produced **one** `[strong] [S]` row for the next code minor (stale `getToolsForProfile` docstring claiming a retired deprecation shim "is preserved below"; same shape as the 2.46.0 retrieval-Composer fix) — ✅ resolved at 2.47.0.1 (bundled with the sibling-file `context-manager.js` cleanup). Updated 2026-05-14.
4. ~~**`git-providers/base.js` 55-method base interface**~~ — ✅ shipped at `RE-EVAL following 2.49.0` at [`docs/ICD-git-providers.md`](ICD-git-providers.md). Covers `BASE_GIT_PROVIDER` (55 methods + 1 `get capabilities()` getter) + 4 concrete providers (github / gitea / gitlab / local) + circuit breaker + health probe under one inheritance-via-shallow-spread contract; 5 classification axes (Implementation / Capability / Error-translation / Circuit-breaker / Contribution); three default-modes pinned (`notSupported` throw / safe-empty / functional default). Stale "43-method" figure corrected: actual surface is 55 methods + 1 getter (drift accumulated since 1.1.0 minimum-viable extraction). Code-aware findings produced **three** items: (a) ~~`getCommitDiff` informal-extension across three remote providers, missing from base~~ ✅ resolved at 2.50.0 (promoted to `BASE_GIT_PROVIDER` with `notSupported` default); (b) ~~GitLab partial-capabilities shape test (anti-regression for the `undefined → false` invariant)~~ ✅ resolved at 2.50.0 ([`tests/test-provider-capabilities-shape.mjs`](../tests/test-provider-capabilities-shape.mjs) + GitLab `get capabilities()` declares all six flags explicitly); (c) `resolveReviewThread` dead-code decision (promote vs demote) — **still open** post-2.50.0; requires architecture-decision session. Updated 2026-05-14.
5. ~~**Retrieval manager + ingest pipeline + lifecycle + persistence**~~ — ✅ shipped at `RE-EVAL following 2.52.0` at [`docs/ICD-retrieval-manager.md`](ICD-retrieval-manager.md). Covers `js/intelligence/retrieval/manager.js` (production singleton, 1264 LOC, 19 public methods + `_resetForTesting`) + pure-DI ingest factories (`loader.js` + `pipeline.js` + `embedder.js` + `store.js` + `ingest-controller.js` + `walker.js`) + production wiring (`wiring.js`) + 3 strategies (`semantic.js` + `structural.js` + `thematic.js`) + measurement harness (`measurement.js` + `comparison.js` + `test-corpus.js`) + event seam (`context:*`, `retrieval:turn-stats`); 5 classification axes (Ingest / Lifecycle / Persistence / Query / Diagnostics); 7 event-driven lifecycle transitions; 6-invariant ingest-pipeline update protocol; IDB key namespace (`retrieval-chunks-` prefix; per-branch). Composer algorithm explicitly out of scope (covered by ICD #2). Code-aware findings produced **three** items: (a) `CODER_V1.task_ledger.capacity` direct read at [`manager.js:678`](../js/intelligence/retrieval/manager.js) survived the 1.20.0 retrieval-config rewire — `resolveTaskLedgerConfig` resolver suggested for next code minor's `[strong]` row, mirrors the 1.20.0 `resolveRetrievalConfig` pattern one-for-one; (b) 12 `@ts-ignore` annotations cluster on store calls — widen `ChunkStore` typedef to absorb (queued, not promoted; mechanical typedef edit but touches downstream references); (c) `retrieval:turn-stats` event has no shape-pinning test — pin via the same idiom as 2.50.0's `tests/test-provider-capabilities-shape.mjs` (queued; browser-DOM-coupled, needs JSDOM fake or manager-side seam). Updated 2026-05-15.
6. **Remaining subsystems** — editor instance, MCP bridge, plugin lifecycle. Profiles registry deferred per `project_profile_admission_paper` memory — paper in flight at [`discussion/`](discussion/). Named at the re-eval slot where each rises to the next-3 window. Next slot target #6 candidate selection at `RE-EVAL following 2.55.0`.

**Trade-off.** ~1–2 hours per subsystem × ~6–8 subsystems = ~12–16 hours of architecture-session time, spread across re-eval slots over weeks. Cost is bounded; each session is small.

---

## Forward ICD presence check

Per methodology §3.1, every `[strong]` milestone in the next-N window must have a contract (ICD or DESIGN doc) on hand. If the contract is missing, the milestone is not actually strong; either demote it (acknowledging the contract gap) or schedule a contract-authoring slot ahead of the milestone.

| `[strong]` milestone in next-3 window | Contract | Status |
|---|---|---|
| ~~**2.43.0 — Git-provider `panels` manifest → rail-views migration**~~ | `audit-2026-Q2/inventory.md` §sidebar/rail (`[REG] [M] [likely]`) + `DESIGN-git-providers-and-ui-extensions.md` §4 decisions table | ✅ shipped 2.43.0 (entries deleted; rail-views seam regression-guarded by `tests/test-slot-manager.mjs`) |
| ~~**2.44.0.N audit-sweep wave** (4 slices)~~ | `docs/audit-2026-Q2/inventory.md` is the running queue | ✅ shipped 2.44.0 — wave-close at 2.44.0.3 with 22 entries closed across the arc; inventory remains live for next sweep wave |
| ~~**2.45.0 — `RE-EVAL following 2.44.0`**~~ | `/config/Projects/plinth/docs/METHODOLOGY-llm-assisted-development.md` §Phase 3 + `docs/ICD-intelligence-composers.md` | ✅ shipped 2.45.0 — ARCHITECTURE.md catch-up sync + ICD #2 + paper-half output (one `[strong]` row queued for 2.46.0) |
| ~~**2.46.0 — retrieval Composer stale docstring fix**~~ | [`ICD-intelligence-composers.md`](ICD-intelligence-composers.md) §"Code-aware findings" | ✅ shipped 2.46.0 — docstring lines 31–35 replaced + audit pass confirmed lines 31–34 were the only drift in `composer.js`; ~12 sibling-file `context-manager.js` references deferred to 2.47.0 |
| ~~**RE-EVAL following 2.46.0 — third re-eval slot (doc-only, no version bump)**~~ | `/config/Projects/plinth/docs/METHODOLOGY-llm-assisted-development.md` §Phase 3 + `docs/ICD-tool-registry.md` | ✅ ran 2026-05-14 — ICD #3 authored + ARCHITECTURE.md catch-up sync 2.44.0 → 2.46.0 + paper-half drift fixes (policy refinement: re-eval slots don't consume a version slot); deliverables accumulate in `[Unreleased]`; one `[strong] [S]` row queued for 2.47.0 (stale `getToolsForProfile` docstring) |
| ~~**2.47.0 — runtime regression fixes (issue-list Start button + `Providers.enrichModels` facade gap)**~~ | User diagnostic dump from live 2.46.0 session + grep-verified call-site analysis | ✅ shipped 2.47.0 — `js/app.js` callback inlined; `js/core.js` facade gained `enrichModels` delegation; two regression tests added. Pre-approved docstring-cleanup row queue-jumped, re-scoped to 2.47.0.1. |
| ~~**2.47.0.1 — sibling-file `context-manager.js` reference cleanup + stale `getToolsForProfile` docstring fix**~~ | [`docs/ICD-tool-registry.md`](ICD-tool-registry.md) §"Code-aware findings" + the 2.46.0 audit pass's out-of-scope finding | ✅ shipped 2026-05-14 — 15 retrieval-module files + `js/tools/registry.js` lines 253–271 docstring rewrite; 27 stale references caught up to the 1.5.14 cutover with past-tense / ICD-cross-referenced phrasing; mirrors the 2.46.0 retrieval-Composer fix shape one-for-one. Absorbed the third RE-EVAL slot's `[Unreleased]` content. |
| ~~**2.48.0 — tool-loop core extraction (github#24 Phase 0)**~~ | [`docs/DESIGN-sub-agents.md`](DESIGN-sub-agents.md) §"Phase 0 — Audit-sweep prereq" | ✅ shipped 2026-05-14 — `handleGeneralRequest`'s iterative tool loop refactored into reusable `runToolLoop(context, hooks, transport)` in [`js/chat/tool-loop-core.js`](../js/chat/tool-loop-core.js) (NEW, ~720 LOC) so Phase 1 sub-agents (target 2.49.0) can drive the same loop with a different context + hooks bag. 13 DOM/state-coupling sites in [`js/chat/handlers.js`](../js/chat/handlers.js) collapsed onto hooks; `handleGeneralRequest` shrunk from ~820 to ~210 lines. 22 pinning subtests at [`tests/test-tool-loop-core.mjs`](../tests/test-tool-loop-core.mjs). No behavior change. |
| ~~**2.48.0.1 — file-edit tool surface (gitea#415)**~~ | AAR of a `qwen-3-6-plus` session on [HTML-Games#212](https://git.gobha.me/xcaliber/HTML-Games/issues/212) | ✅ shipped 2026-05-14 — three fixes bundled around `edit_file` error feedback, description clarity, and validateToolParameters hypothesis-stripping. Two new tests + 3 added to the refusal-hint suite; 20/20 pass. |
| ~~**2.49.0.0 — sub-agents Phase 1 slice 1 (github#24 Phase 1 slice 1, gitea#418)**~~ | [`docs/DESIGN-sub-agents.md`](DESIGN-sub-agents.md) §"Slicing rationale" | ✅ shipped 2026-05-14 — pure plumbing; zero user-observable surface. `subagent.v1` profile + resolver + registry entry-points + state slot + classification + filter. 23 new subtests across 3 test files. |
| ~~**2.49.0 — sub-agents Phase 1 end-to-end (github#24 Phase 1 slice 2)**~~ | [`docs/DESIGN-sub-agents.md`](DESIGN-sub-agents.md) full doc | ✅ shipped 2026-05-14 — the `delegate_task` tool + `SubAgentApprovalCard` pair + `SubAgentTranscriptPanel` + `coder.v1.tools.static` admission + `SUBAGENT_INSTRUCTIONS` prompt block + cost-attribution threading + sub-agent runner glue + persistence + Settings UI row + browser test. End-to-end; tag `v2.49.0` fired here. |
| ~~**RE-EVAL following 2.49.0 — fourth re-eval slot (doc-only, no version bump)**~~ | `/config/Projects/plinth/docs/METHODOLOGY-llm-assisted-development.md` §Phase 3 + [`docs/ICD-git-providers.md`](ICD-git-providers.md) | ✅ ran 2026-05-14 — ICD #4 authored ([`docs/ICD-git-providers.md`](ICD-git-providers.md), `BASE_GIT_PROVIDER` 55-method surface + 4 concrete providers + circuit breaker; 5 classification axes; three default-modes pinned) + paper-half drift fixes (this row + ROADMAP "Now"/"Just shipped" roll-forward + 43-method → 55-method fix in ROADMAP + ARCHITECTURE + DESIGN-git-providers forward-pointer); deliverables accumulated in `[Unreleased]` and were absorbed at 2.50.0. Three `[strong]`-band code-aware findings queued; findings #1 + #2 ✅ resolved at 2.50.0; finding #3 (`resolveReviewThread` decision) still open. |
| ~~**2.50.0 — ICD #4 code-aware findings #1 + #2 (`getCommitDiff` base-promotion + provider capabilities-shape test)**~~ | [`docs/ICD-git-providers.md`](ICD-git-providers.md) §"Code-aware findings" | ✅ shipped 2026-05-14 — `BASE_GIT_PROVIDER.getCommitDiff` `notSupported` stub at [`base.js:637-650`](../js/git-providers/base.js); [`tests/test-provider-capabilities-shape.mjs`](../tests/test-provider-capabilities-shape.mjs) pins the six-flag contract across 5 provider surfaces; GitLab `get capabilities()` declares all six flags explicitly; [`tests/test-pr-review-provider-shape.mjs`](../tests/test-pr-review-provider-shape.mjs) updated in lockstep. Absorbs the `RE-EVAL following 2.49.0` `[Unreleased]` content. |
| ~~**2.50.0.1 — Anthropic-style prompt-cache breakpoint on Venice requests (gitea#423)**~~ | AAR of `qwen-3-6-plus` session against [HTML-Games#215](https://git.gobha.me/xcaliber/HTML-Games/issues/215) — ~209k tokens / ~23% session were Composer-trimmed tool-defs prefix re-billed every request | ✅ shipped 2026-05-15 — [`js/providers/venice.js`](../js/providers/venice.js) `_attachSystemCacheBreakpoint` wraps the last system message's content for Venice's Anthropic-style prefix cache. `enablePromptCache` setting default ON; 7 new subtests at [`tests/test-venice-prompt-cache.mjs`](../tests/test-venice-prompt-cache.mjs). |
| ~~**2.50.0.2 — tool-surface UX (gitea#422)**~~ | AAR cohort — three tools each cost a round-trip when called with cross-SDK-prior arg names | ✅ shipped 2026-05-15 — `TOOL_PARAM_ALIASES` frozen export + `applyAliasesAndDefaults` pre-validator rewrite; `**Required:** …` description prefix on all 18 classified tools; `create_file` validator-handler contract collapse. Two new test files + one new subtest in `test-chat-tools-validation-message.mjs`. |
| ~~**2.50.0.3 — cross-request read-cache returns full payload (gitea#421)**~~ | AAR cohort — model re-issued reads when cached results returned a `(cached)` marker rather than the payload | ✅ shipped 2026-05-15 — the `_cache_note` path on stateful reads now returns the full cached payload when args match. Same antibody as `project_edit_file_stale_cache_deadlock` 3rd-instance. |
| ~~**2.51.0 — compact tools TOC in system prompt (gitea#426)**~~ | AAR cohort — model repeated `list_tool_categories` / `list_tools_by_category` calls because the system prompt's tool enumeration didn't surface them | ✅ shipped 2026-05-15 — verbose per-tool prose replaced with a compact category index in the stable head; full per-tool detail still available via discovery tools. |
| ~~**2.51.0.1 — session cut-off reason surfaced (gitea#425)**~~ | AAR cohort — users couldn't distinguish budget-exceeded from model-finished | ✅ shipped 2026-05-15 — structured `reason` field on trailing assistant message + status pill (`budget-exceeded` / `model-finished` / `user-cancelled` / `tool-cancelled` / etc.). |
| ~~**2.52.0 — `read_approved_plan` tool + persisted plan body (gitea#424)**~~ | AAR cohort — model regenerated identical DESIGN.md content from inlined plan body, paying tokens twice | ✅ shipped 2026-05-15 — `State.approvedPlan` slot persists the approved plan; new read-only `read_approved_plan` tool surfaces it to the executor; closes the last open AAR-cohort issue. |
| ~~**RE-EVAL following 2.52.0 — fifth re-eval slot (doc-only, no version bump)**~~ | `/config/Projects/plinth/docs/METHODOLOGY-llm-assisted-development.md` §Phase 3 + [`docs/ICD-retrieval-manager.md`](ICD-retrieval-manager.md) | ✅ ran 2026-05-15 — ICD #5 authored ([`docs/ICD-retrieval-manager.md`](ICD-retrieval-manager.md)) + ARCHITECTURE.md catch-up sync 2.49.0 → 2.52.0 + administrative sweep (closed gitea#418/#424/#425/#426) + paper-half drift fixes; deliverables accumulate in `[Unreleased]`. Three `[strong]`-band code-aware findings queued (one for next code minor: `resolveTaskLedgerConfig`; two queued). |
| ~~**2.55.0 — default-OFF dev warning when newly-registered tools aren't admitted by any profile (gitea#439)**~~ | `discussion/profiles-pick-tools.md` §Decision Thread 4 + §Follow-up #2 + 2.54.0's CHANGELOG entry explicit "registry-side warning surfaces it (gitea#439)" hook | ✅ shipped 2026-05-15 — `Profiles.findAdmittingProfiles(toolName, { overlayNames? }) → string[]` co-located with `filterTools` in [`js/profiles/registry.js`](js/profiles/registry.js); `ToolRegistry.register` wire-up at [`js/tools/registry.js`](js/tools/registry.js) on the fresh-register branch only. `'*'` sentinel deliberately excluded (silent-vanish guard); `overlayNames` seam forward-compat for gitea#442. 17 new subtests across 2 new test files at [`tests/test-profile-find-admitting-profiles.mjs`](../tests/test-profile-find-admitting-profiles.mjs) + [`tests/test-tool-registry-admit-warning.mjs`](../tests/test-tool-registry-admit-warning.mjs); full suite 3467 pass / 1 skipped / 0 fail. Unblocks regression-safe execution of gitea#440. |
| Touch 3 Window v2 / Sessions | None — `docs/DESIGN-sessions.md` will need authoring | `[medium]` retained at `RE-EVAL following 2.52.0` — 2.0.0 dependency cleared (the Touch 3 design's hard prerequisite); 6 architecture questions parked in [`discussion/touch-3-window-v2-sessions.md`](discussion/touch-3-window-v2-sessions.md). Promotion to `[strong]` blocked on DESIGN-sessions.md authoring slot. |
| MCP discovery Phase 2 OAuth flows ([github#27](https://github.com/gobha-me/ai-editor/issues/27)) | None — `docs/DESIGN-mcp-oauth.md` will need authoring | `[medium]` retained at `RE-EVAL following 2.52.0` — security surface warrants paper-first contract; one external request is enough pull given the OAuth contract implications. DESIGN-mcp-oauth.md authoring slot queued (no specific milestone target yet). |
| ~~**Profile-admission paper (github#40)**~~ | ~~`discussion/profiles-pick-tools.md` + `discussion/plugin-dev-mode-vs-profile.md` + `discussion/user-built-profile-trees.md`~~ | ✅ paper closed 2026-05-15; six follow-up tickets filed (gitea#438–#443). **gitea#438 shipped at 2.54.0** (admission inverted: `tools.admit` enumerates explicit tool names; `'<prefix>__*'` glob for MCP; `admit_add`/`admit_remove` inheritance operators; `getKnownGroupTags` + `LEGAL_GROUP_TAGS` + `_registeredRoles` retired). **gitea#439/#440/#442 unblocked** for the next code minor cohort (default-OFF dev warn / hand-curate admit / `plugin.enabled` overlay flag). gitea#441 (`Roles`→`Profiles` UI rename) and gitea#443 (Phase 4 user-authoring API placeholder) remain independent. Profiles registry still deferred as ICD candidate — the admission code is now invertable to its post-2.54.0 shape, so the next re-eval can author the ICD against settled semantics. |

Translation post-`RE-EVAL following 2.52.0`: the **AAR cohort closed** across 2.50.0.1 → 2.52.0 — all six AAR siblings (gitea#421/#422/#423/#424/#425/#426) shipped within a 36-hour window. Four re-evals ran since 2.42.0 (the methodology adoption): **first** at 2.42.0 (ICD #1, paper-half), **second** at 2.45.0 (ICD #2, one `[strong]` row → 2.46.0), **third** as `RE-EVAL following 2.46.0` (ICD #3, one `[strong]` row → 2.47.0.1; established the no-version-bump-for-re-evals policy refinement at Decision §14 sub-clause), **fourth** as `RE-EVAL following 2.49.0` (ICD #4, three `[strong]` candidates → 2.50.0 absorbed #1+#2; #3 still open). **Fifth** as `RE-EVAL following 2.52.0` (this slot; ICD #5 retrieval manager authored; three `[strong]`-band candidates surfaced — one suggested for the next code minor's `[strong]` row: `resolveTaskLedgerConfig`; two queued). The 2.50.0 → 2.52.0 patch-cohort path shipped the entire AAR cohort plus ICD #4 findings #1+#2; the next code minor (2.53.0) is the absorption slot for this re-eval's `[Unreleased]` content. The three named near-term features (Window v2 / Sessions, MCP OAuth, profile-admission paper) stay `[medium]` with DESIGN-doc-authoring (Sessions / MCP OAuth) or paper-in-flight (admission) as their promotion preconditions. ICD #4 finding #3 (`resolveReviewThread` decision) still requires an architecture-decision session.

---

## 2.X path — what's still open

The 1.14.0 → 2.0.0 profiles arc shipped end-to-end through 14 slices (1.14.0 → 2.0.0, including parallel 1.X tracks for Sandbox/LLM-authored automation, Plugin Discoverability, Tier-1/2/3a preview, Retrieval ingest hardening). **See [CHANGELOG.md](../CHANGELOG.md) for per-slice rationale.** The verification footing (§Decisions §7 Removability check) held: every slice's profile-read could be replaced with the prior hardcode and the regression suite passed; the user-visible slices (1.15.0 ledger markers, 1.17.0 `preserve_recent` reconciliation, 1.21.0 picker UI, 2.0.0 role-removal) each have explicit regression tests.

**Still open:** **2.0.x stabilization patches** `[medium]`. Advanced view of the picker (forked profile definitions, raw `Profile` struct edits per [`DESIGN-profiles.md`](DESIGN-profiles.md) line 587 Two-View Configuration); migration-script edge cases; per-profile diagnostics polish. Power-user surface, deferrable without breaking the 2.0 load-bearing claim. No specific ICD yet; will tighten when a real edge case surfaces.

---

## After 2.0.0 — Phase 2/3/4 continuation

| Slice | Maps to design | What ships | Band |
|---|---|---|---|
| ~~Phase 2 (data + harness) shipped 2.6.0; `kb.v1` picker-promoted 2.8.0~~ | `DESIGN-profiles.md` line 455 | *see [CHANGELOG §2.6.0](../CHANGELOG.md) and [§2.8.0](../CHANGELOG.md)* | — |
| **Phase 2 picker promotion — `chat_multi.v1` / `rp.v1`** | `DESIGN-profiles.md` | Profiles target consumers (multi-user shared chat, role-play personas) that don't exist in ai-editor; promotion needs a different consumer (custom plugin profile inheriting `base: 'rp.v1'`, or a separate product surface) — that's the Phase 4 authoring API path. Picker-list tests pinned at `['chat.v1', 'coder.v1', 'kb.v1']`. | `[fuzzy]` (no real consumer; deprioritized for ai-editor) |
| **2.3.0 Phase 3 — operational maturity** | `DESIGN-profiles.md` | Task boundary detection heuristics (replaces explicit markers), novelty-score tuning from real usage, per-profile dashboards. | `[fuzzy]` (no ICD; gated on Phase 2 dogfood signal) |
| **2.4.0+ Phase 4 — extensibility** | `DESIGN-profiles.md` | Custom profile authoring API, profile diffing, profile regression testing harness. Unblocks plugin-defined surfaces. | `[fuzzy]` (no ICD; gated on Phase 3) |

---

## Parallel work streams

| Stream | Status | Band |
|---|---|---|
| ~~LLM-authored automation (Tier 0 sandbox + Plan-Mode-shaped approval card)~~ | ✅ Phase 1 shipped 1.16.0; validated against HTML-Games (~200× cost collapse vs. manual). Phases 2–5 park behind real usage data per [`DESIGN-llm-authored-automation.md`](DESIGN-llm-authored-automation.md). | — |
| ~~Plugin Discoverability~~ | ✅ shipped 2.1.0 | — |
| ~~In-editor preview Tier 1 (sandboxed iframe + Service Worker)~~ | ✅ shipped 1.22.0 | — |
| ~~In-editor preview Tier 2 (console + error capture)~~ | ✅ shipped 2.7.0; closed the Sokoban class on HTML-Games | — |
| ~~In-editor preview Tier 3a (driveable preview, selector-shaped)~~ | ✅ shipped 2.10.0; `preview_eval` decision settled (does not ship); `preview_screenshot` deliberately deferred | — |
| **In-editor preview Tier 3b (sidecar + build-step support)** | Playwright (or equivalent) sidecar per workspace; container isolation; per-workspace `npm run dev` lifecycle. Closes the Cogfall (Vite/TS/Pixi) class. **Gating mechanism not yet operational** — needs a probe that classifies "this bug would have been served by Tier 3b but not Tier 3a + Tier 2 + Tier 1." Independent of the Profiles arc. | `[fuzzy]` (gating mechanism undefined) |
| ~~Retrieval ingest hardening (delta-indexing 2.2.0; language-stats + token cap 2.4.0)~~ | ✅ shipped; track closed | — |

---

## 2026-Q2 code audit + sweep track ~~`[strong]`~~ (✅ closed 2026-05-14 at 2.44.0)

> *Started 2026-05-11 post-2.23.0 SlotManager migration. Measurement-first sweep — read the codebase, catalog refactor candidates, burn them down at ~1 entry per spare slot. Closed 2026-05-14 at 2.44.0 (PR #408 wave-close) after 22 entries resolved across 11 system buckets.*

Living inventory: [`docs/audit-2026-Q2/inventory.md`](audit-2026-Q2/inventory.md) — retained as historical record + queue for the next sweep wave whenever the next audit pass starts. Categories (**HC** hardcode wall · **EV** missing event wiring · **DUP** duplicate implementation · **REG** should-be-registered-isn't · **ST** style drift), system buckets, confidence tags, and per-entry touch points all live there.

**Closure summary.** Two waves shipped in X.Y.Z.N space: the 2.33.0 → 2.39.0 wave (orphan-emit hygiene + early sweep entries; closed 2.39.0 per `js/version.js` `.N` strip) and the 2.44.0 wave (4 slices: tool-name string-literals, `safeAdd` registry, settings-tab activation, `managers/` placement; closed 2.44.0 with three remaining open entries triage-closed as no-op-or-precondition-not-met). The X.Y.Z.N wave shape (one tag covering many sub-step closures) is now established precedent for future sweep waves.

**Triage policy + sizing.** Confidence-tag rules (likely → ship; needs-investigation → audit-first; maybe-intentional → designate as public extension API or delete). Sizing: **[S]** (single PR, <100 LOC) folds into in-track patches; **[M]** earns its own minor slot; **[L]** earns a design doc first. Closure threshold: fewer than ~5 entries remain that survive triage — fired 2.44.0.3.

---

## Deferred / parked

> **Why this section exists.** Foundations (was 1.1.x) and Compression (was 1.2.x) were sequenced before Memory + Tools jumped ahead. Some items are gated on metrics the cost dashboard produces (shipped 1.2.1; export shipped 1.6.6). Some may be obsolete now that adjacent tracks shipped their equivalents (e.g. `TaskLedger` landed in 1.3.17). Sorting paused-vs-abandoned is part of each re-evaluation pass.
>
> **Three buckets per methodology.** Items here are either `[fuzzy]`-band parked (still visible on the roadmap; preconditions named or unknown), pre-architecture thinking in [`docs/discussion/`](discussion/), or eventually slipped past the planned horizon (`docs/deferred/` — not bootstrapped; allocate when needed).

### `[fuzzy]` parked-but-named

**Foundations (was 1.1.x)** — `[fuzzy]` until a precondition fires:

| Item | Trigger to promote |
|---|---|
| Turn metadata enrichment | Compression Rules 1–3 ship; their measurement loop needs `file_ops` + `tool_result_for` coverage |
| Migration coverage probe | Same — gated alongside Rules 1–3 |
| Pre-merge version coherence check (CI lint) | Recurring drift incident; one evening to fix forever |
| Embedder hardening (provider decoupling, filetype filters, in-browser embedder validation, Settings → Embeddings tab) | Real-repo measurement showing the file-count ceiling or filetype noise is dominating cost |
| Vim keybindings | User demand |
| Glassworm / Trojan-Source protection (CI lint + editor decoration + plugin-install warning) | Already partially addressed at 2.17.1 (tool-return scan); remaining work gated on a real attack class |

**Compression (was 1.2.x)** — `[fuzzy]` post-Rules-1+2 measurement:

| Item | Trigger to promote |
|---|---|
| Rules 1+2 (Subsumption, Invalidation) | Cost-dashboard export landed 1.6.6 → unblocked; ship-then-validate per `project_cost_quality_tradeoff` memory |
| Rule 3 (Consumption) | ≥95% `tool_result_for` coverage on production sessions |
| Rule 4 (Resolution) | Rule 3 numbers matching design |
| Rule 5 tuning | Pipeline plumbing existing summarizer cleanly |
| Settings → Compression panel refresh | Rules 1–5 live |

**Other deferred (banded inline):**

- PR Review polish follow-ups → moved to [`discussion/pr-review-polish.md`](discussion/pr-review-polish.md). `[fuzzy]` (gated on usage signal post-2.14.0).
- Chat panel facelift (three Touch 2 variants) — `[fuzzy]` until a direction is picked or roll into the next major.
- Persona memory scope — `[fuzzy]` (deferred indefinitely; workspace + user scopes cover demand).
- In-app help renderer (sidebar pane instead of modal) — `[fuzzy]`.
- Mobile secondary pane rework — `[fuzzy]`.
- Issue/PR tab visual hierarchy — `[fuzzy]` (lo-pri).
- Plugin marketplace — `[fuzzy]` (defer to a major post-2.x once the architecture stabilizes).
- Plugin settings panel tab — `[fuzzy]` (richer config UI beyond auto-generated `configSchema`).
- CodeMirror extension bridge — `[fuzzy]` (expose `EditorView` to plugins).
- Tools settings page — `[fuzzy]` (dedicated tab showing all registered tools).
- Custom role creation UI — `[fuzzy]` (post-2.0; profiles changed the picture).
- Cross-project tools (`peek_scan_file`, `peek_search_in_files`, `peek_read_function`) — `[fuzzy]` (needs cross-repo provider work first).
- More languages in `scan_file` (Go, Rust, Java, C/C++) — `[fuzzy]`.
- Expand `.mjs` test coverage — `[fuzzy]` (port browser-only suites to `node:test`).
- Generic / custom git provider — `[fuzzy]`.
- Offline / PWA support — `[fuzzy]`.

### Pre-architecture thinking → `docs/discussion/`

Three discussion docs are seeded; future discussion docs land here as Claude sessions surface architectural questions Jeff isn't yet ready to answer:

- [`discussion/github-37-phase-2.md`](discussion/github-37-phase-2.md) — eight design questions for CLAUDE.md Phase 2.
- [`discussion/pr-review-polish.md`](discussion/pr-review-polish.md) — four PR Review follow-ups.
- [`discussion/touch-3-window-v2-sessions.md`](discussion/touch-3-window-v2-sessions.md) — Window v2 / Sessions architectural seams.

Cite as "see `discussion/X.md` for the thinking," never as a source of truth (per [`discussion/README.md`](discussion/README.md)).

### Deferred — slipped past the planned horizon

`docs/deferred/` is not bootstrapped yet. Allocate when the first item graduates from `[fuzzy]` to "preconditions slipped past horizon; preserved but inactive."

---

## Known open issues — not yet scheduled

User-facing gaps tracked as filed issues but not yet slotted into a track. **Issue trackers split by audience:** internal/dogfood-only on Gitea (`git.gobha.me/xcaliber/ai-editor`); public-facing on the GitHub mirror (`github.com/gobha-me/ai-editor`).

**Open:**

| Issue | Summary | Band | Rationale |
|---|---|---|---|
| [github#37 Phase 2](https://github.com/gobha-me/ai-editor/issues/37) | Re-scope CLAUDE.md analogue from dogfood signal | `[medium]` if dogfood signal arrives; `[fuzzy]` until then | Phase 1 may turn out to be the whole answer. 8 design questions parked at [`discussion/github-37-phase-2.md`](discussion/github-37-phase-2.md). |
| [github#27 Phase 2 OAuth flows](https://github.com/gobha-me/ai-editor/issues/27) | MCP server OAuth | `[medium]` (retained at 2.45.0 re-eval; security surface warrants paper-first contract) | Sized; `docs/DESIGN-mcp-oauth.md` authoring slot queued (no milestone target yet). Picker-list tests stay pinned. |
| [github#24 Sub-agents Phase 1](https://github.com/gobha-me/ai-editor/issues/24) | Bounded child conversations | `[medium]` (DESIGN-sub-agents.md shipped 2.37.0; Phase 0 audit-sweep prereq + post-2.0 gating before promotion to `[strong]`) | 6-phase rollout defined; Phase 0 = audit-sweep PR extracting tool-loop core. |
| [github#18 Cross-device sync](https://github.com/gobha-me/ai-editor/issues/18) | Settings sync via QR/P2P | `[fuzzy]` (designed in [`DESIGN-cross-device-sync.md`](DESIGN-cross-device-sync.md); post-2.0 by design) | "Cross-process / distributed state" is out of scope for 1.x→2.0; this is the design-on-paper edge. Revisit post-profile-contract. |

**Closed:** see [CHANGELOG.md](../CHANGELOG.md) — every previously-listed closed issue is documented there. Removed from this doc on 2026-05-12 to drop ~70 lines of duplicated history (CHANGELOG remains authoritative).

**Closed prior to 2.42.0 (recorded at this re-eval for paper-half drift fix):**

- ~~[gitea#392](https://git.gobha.me/xcaliber/ai-editor/issues/392)~~ — `[ui] Branch switcher: clicking a branch switches it but active highlight/overlay doesn't move`. Closed pre-2.42.0; no in-doc fix-PR ref captured. The 2.41.0 ROADMAP listed this as open `[strong]`; this re-eval's paper-half catches the drift.
- ~~[gitea#393](https://git.gobha.me/xcaliber/ai-editor/issues/393)~~ — `[ui] Left-panel rail views: clicking a group should trigger a refresh`. Closed pre-2.42.0 (resolved by the `view.onActivate(viewId)` shape extension noted in [`js/ui/left-pane-rail.js`](../js/ui/left-pane-rail.js) module header — *"Activation refresh (gitea#393, 2.38.1)"*). The 2.41.0 ROADMAP listed this as open `[strong]`; this re-eval's paper-half catches the drift.

---

## Decisions

> Resolved from discussion. **Strong-band architecture commitments.** New decisions land here only via an architecture session — code sessions cite these, they don't add to them.

1. **Memory storage is two-tier.** Browser cache default (per-tab); repo-committed `.aieditor/memory/*.md` opt-in per workspace. Toggle in Settings → Memory.
2. **Roles got replaced by the profile picker at 2.0.** No dual-surface; one selector. Migration script translated stored `role` to a profile preset. **Shipped 2.0.0.**
3. **Compression diagnostics surface as a public status-bar pill** (e.g., "📉 60% kept"), not just debug-mode. Full eviction trace stays in the LLM debug modal for power users.
4. **Memory files auto-stage on commit when repo mode is opt-in AND the current branch isn't protected.** On protected branches, the memory diff surfaces in the commit modal as an unstageable warning.
5. **Tool budget defaults to 5000 tokens** (per design), exposed as a tunable in Settings → Tools.
6. **Branching: per-PR feature branches off `main`. Squash + delete on merge.** No long-lived track branches.
7. **Removability is an explicit checkpoint, not a vibe.** Every Phase-1 milestone carries a Removability check in its exit criteria. If "subsystem removed → no user-visible degradation," the next minor is gated on closing that gap.
8. **Measurement before scale.** Cost dashboard shipped at 1.2.1; export at 1.6.6 — both unblocking. Each subsequent track lands against measured baselines.
9. **Preact + `htm` allowed for new state-heavy surfaces from 1.3.0; vanilla everywhere else.** Existing tabs / sidebar / file tree / editor frame / chat stay vanilla forever; no migration. Bigger uniform-UI consolidation is a post-2.0 arc.
10. **claude.ai/design engages on a three-touch model.** Touches 1 + 2 shipped (Memory UX → 1.3.0; whole-app facelift → 1.3.5–1.3.13); Touch 3 received 2026-05-07 (left pane + window architecture). Touch 3's Rail v2 / PR Review / Merge Conflict / zip-flow shipped; **Window v2 / Sessions is the only Touch 3 surface left, post-2.0 by design.**
11. **Two-view configuration for every settings panel.** Preset view (intent) + advanced view (parameters), reachable via the same toggle name and position in every panel. Editing in advanced flips the preset selector to "Custom"; switching back to a named preset snaps every knob to that preset's defaults. **No separate "Developer mode" sections.** Full contract in [`DESIGN-profiles.md`](DESIGN-profiles.md) §"Two-View Configuration."
12. **Release-readiness gate (2026-05-04).** Release tag pushes (`X.Y.Z`-shaped; **`X.Y.Z.N` sub-patches don't fire the gate**) require a passing 10-turn dogfood in this repo. See §"Cadence and versioning."
13. **Paper-only planning sessions are scheduled, not ad-hoc.** When the path forward grows vague — sessions answer roadmap questions with "n-z more changes" instead of pinned slices, or the same question gets re-asked across sessions — a **docs-only re-layout pass** holds the active queue before any implementation continues. Output: an updated ROADMAP section with pinned version slices, a verification footing, and a sizing call-out. No code is written in a planning session. **First applied 2026-05-08** in the path-to-2.0.0 re-layout. **The 2026-05-12 methodology adoption (this restructure) is the second application** — same shape, larger scope (added the X.Y.Z.N convention, banded all pending milestones, bootstrapped `discussion/`, scheduled re-eval cadence).
14. **Re-evaluation is a scheduled roadmap item, not an ad-hoc decision (added 2026-05-12).** Every 3 code minors, an inline `RE-EVAL following X.Y.Z [rewrite session]` item gates the next code milestone. Both halves run (paper + code-aware) unless explicitly skipped per methodology §3.3. The sliding-window mechanism is visible to any session reading the roadmap; if a session reaches a re-eval item, it picks up the re-eval before the next code milestone. See §"Re-evaluation cadence" for what each pass does. **Sub-clause (refined at `RE-EVAL following 2.46.0`, 2026-05-14):** Re-eval sessions are doc-only PRs that accumulate in `[Unreleased]`; they do **not** consume a version slot. The version-slot reservation applies to the next code minor, not the re-eval. This composes with the [`feedback_no_bump_for_measurement_only`](file:///config/.claude/projects/-config-Projects-ai-editor/memory/feedback_no_bump_for_measurement_only.md) memory rule — both apply to docs-only / measurement-only changes. The 2.42.0 (first re-eval) and 2.45.0 (second re-eval) doc-only releases were the pre-refinement shape; the rule corrects forward from the third slot onward.

---

## What's out of scope for the 2.x arc

- **Multi-modal context.** Image/audio chunks. Vision models work for images today (paste-to-chat); structured handling is a future-major topic.
- **Cross-process / distributed state.** Stays single-process. No sync server, no multi-tab live coordination beyond the existing tab-isolation model.
- **Real-time collaborative editing.** Two users editing the same file simultaneously. Not a context problem; not in scope.
- **Plugin marketplace.** Defer to a post-2.x major after the profile contract stabilizes against real plugin consumers.
- **Markdown-as-source AST chunking.** Phase 1 code chunker is regex-based; tree-sitter-grade is gated on measured need.
- **Custom embedding model fine-tuning.** Use the user's chosen provider.
- **Cross-project task ledger.** Task ledgers stay session-scoped. Continuity goes through memory with consent.

---

## What this roadmap commits to

- **Now / Next / Later** at the top — the doc's first job is to answer *what's being worked on, what's queued, what's not*.
- **Band labels on every pending milestone.** Unlabeled = `[strong]`; that commitment should feel heavy.
- **Forward cadence, not changelog.** Shipped work and per-PR rationale live in CHANGELOG. This doc describes where we're going.
- **Admissibility, not accumulation** as the architectural principle every PR is reviewed against.
- **Git-native memory and session state** as the externally-tellable story.
- **Measurement before scale.** Cost dashboard is gating infrastructure; tracks that nominally depend on it stay deferred until their precondition fires.
- **Removability discipline.** Every Phase-1 milestone exit criteria asks: with this subsystem removed, what user-visible behavior degrades?
- **Re-evaluation as a first-class roadmap entry.** Every 3 minors. Both halves (paper + code-aware) unless explicitly skipped.
- **The DESIGN docs are the contract.** When implementation diverges from a DESIGN doc, the doc updates first (in an architecture session), then the code.
- **Honest deferrals.** Multi-modal, collaboration, marketplace are not on this roadmap.

Push back on any of this. The roadmap is a hypothesis. The first track that drifts more than two weeks past its target gets the next milestone re-scoped, not the deadline pushed.

---

## See also

- [VERSIONING.md](VERSIONING.md) — X.Y.Z.N convention; composition with the three versioning feedback memories.
- [ARCHITECTURE.md](ARCHITECTURE.md) — module dependency map; per-subsystem detail in `docs/DESIGN-*.md`.
- [CHANGELOG.md](../CHANGELOG.md) — authoritative running history.
- [`docs/discussion/`](discussion/) — pre-architecture thinking.
- [`docs/audit-2026-Q2/inventory.md`](audit-2026-Q2/inventory.md) — sweep queue.
- [`docs/dogfood-battery/`](dogfood-battery/) — measurement battery.
- Plinth methodology source: `/config/Projects/plinth/docs/METHODOLOGY-llm-assisted-development.md`.
